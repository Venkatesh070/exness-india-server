PORT=4000

FRONTEND_URL=http://localhost:8080

# PostgreSQL
DATABASE_URL=postgresql://exness:exness_local@localhost:5432/exness_india
DATABASE_SSL=false

# Firebase — only FIREBASE_PROJECT_ID is required for JWT verification on the server
FIREBASE_PROJECT_ID=exness-india

# Optional (only if using server-side Firebase REST/Admin features)
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY=
FIREBASE_WEB_API_KEY=
